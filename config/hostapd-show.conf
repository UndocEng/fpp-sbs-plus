interface=wlan1
driver=nl80211
ssid=SHOW_AUDIO
hw_mode=g
channel=11
country_code=US
wmm_enabled=1
ieee80211n=1
auth_algs=1
ignore_broadcast_ssid=0

# Open WiFi - no password for easy audience access
wpa=0

# AP isolation - phones can only talk to the Pi, not each other
ap_isolate=1
